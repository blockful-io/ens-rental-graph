{
  "name": "ens-rental",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ens-rent",
    "create-local": "graph create --node http://localhost:8020/ ens-rent",
    "remove-local": "graph remove --node http://localhost:8020/ ens-rent",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ens-rent",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.89.0",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}